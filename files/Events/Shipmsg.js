module.exports = {
	name: 'Shipmsg',
	async execute(msg, pool) {
		//const res = await pool.query(`SELECT * FROM ship WHERE userid = '${msg.author.id}' AND guildid = '${msg.guild.id}'`);
		//if (res) {
		//	if (res.rows[0]) {
        //        const r = r.rows[0];
         //       const mpos = r.mentionspos;
          //      const mneg = r.mentionsneg;
        //        
		//	} else {

        //    }
	//	}
	}
};